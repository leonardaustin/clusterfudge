apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: e2e-deny-all-ingress
  namespace: clusterfudge-e2e
  labels:
    managed-by: clusterfudge-e2e
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  # No ingress rules = deny all ingress
