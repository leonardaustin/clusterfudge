apiVersion: apps/v1
kind: Deployment
metadata:
  name: e2e-nginx
  namespace: clusterfudge-e2e
  labels:
    app: e2e-nginx
    managed-by: clusterfudge-e2e
spec:
  replicas: 1
  selector:
    matchLabels:
      app: e2e-nginx
  template:
    metadata:
      labels:
        app: e2e-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "64Mi"
