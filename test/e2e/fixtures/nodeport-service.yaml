apiVersion: v1
kind: Service
metadata:
  name: e2e-nodeport-svc
  namespace: kubeviewer-e2e
  labels:
    managed-by: kubeviewer-e2e
spec:
  type: NodePort
  selector:
    app: e2e-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
